//! CLI entrypoint: arg/mode parsing, preview/confirm UX, output rendering.

fn main() {
    println!("enShell — the AI-native shell assistant");
    println!("(No functionality yet: planning stage)");
}

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        // Placeholder — real CLI tests will live here.
        assert_eq!(2 + 2, 4);
    }
}
